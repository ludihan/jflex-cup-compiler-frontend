# Build
## Linux / BSD / MacOS / Unix:
use any posix shell:
```sh
sh build.sh build && sh build.sh run
```
## Windows:
use bash (comes with git on windows):
```sh
sh build.sh build && sh build.sh run
```
